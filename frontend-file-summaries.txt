{
  "frontend/src/index.css": "Global OKLCH design tokens for both dark (default) and light themes, custom fonts, scrollbar styling, and message bubble utilities.",
  "frontend/tailwind.config.js": "Tailwind config with darkMode: 'class', full OKLCH color token mapping, Xeta custom tokens, Nunito/Inter fonts, bubble border-radius, and green glow shadows.",
  "frontend/src/hooks/useBluetooth.ts": "Custom React hook wrapping the Web Bluetooth API — now uses the locally declared types from web-bluetooth.d.ts, removing the manual Navigator cast since the ambient declaration extends Navigator directly with the bluetooth property.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations — `useCreateUser` now also destructures `isFetching` from `useActor` and throws a user-friendly \"Connection not ready\" error if the actor is still initializing or null at mutation time, with a try-catch around the canister call to handle transient network errors gracefully.",
  "frontend/src/components/ConnectionStatus.tsx": "Persistent status bar showing Bluetooth connection state with animated indicator dot, device name, and connect/disconnect action buttons.",
  "frontend/src/components/MessageBubble.tsx": "WhatsApp-style message bubble rendering sent (right, green) and received (left, gray) messages with text/image/file support, timestamps, and receipt icons.",
  "frontend/src/components/BluetoothPairing.tsx": "Full-screen pairing UI with animated scan rings, Bluetooth logo, signal bar animation, browser support detection, error display, and a \"Scan for Devices\" button — now wrapped in a scrollable outer container (`overflow-y-auto`) with an inner `min-h-full` flex column so all content is reachable via scrolling on small viewports while remaining centered when space allows.",
  "frontend/src/components/ChatInterface.tsx": "Main chat view with auto-scrolling message list, message input with Enter-to-send, send button with loading state, and a clear-conversation dialog — integrates both Bluetooth sending and backend persistence.",
  "frontend/src/App.tsx": "Root app component with auth/profile gating, tab navigation, chat routing, and initTheme() called at module level to prevent theme flash — uses named imports for all existing components and default imports for new ones.",
  "frontend/src/types/web-bluetooth.d.ts": "Local TypeScript ambient declaration file that defines all necessary Web Bluetooth API types (BluetoothDevice, BluetoothRemoteGATTServer, BluetoothRemoteGATTService, BluetoothRemoteGATTCharacteristic, Bluetooth, and the Navigator extension) since @types/web-bluetooth is not installed.",
  "frontend/index.html": "HTML entry point with page title updated to \"Xeta\".",
  "frontend/src/utils/formatters.ts": "Utility functions for formatting relative time, presence status (online/last seen), initials extraction, message timestamps, and chat list timestamps.",
  "frontend/src/components/PresenceIndicator.tsx": "Small component displaying a colored dot and \"Online\" or \"Last seen X ago\" text based on a lastSeen timestamp.",
  "frontend/src/components/ReceiptIcon.tsx": "SVG receipt icon component rendering single checkmark (sent), double gray checkmark (delivered), or double green checkmark (read) based on MessageStatus.",
  "frontend/src/components/UserAvatar.tsx": "Reusable avatar component showing an image or a colored initials placeholder with consistent hue based on display name.",
  "frontend/src/components/RegistrationScreen.tsx": "Full-screen registration form that now imports `useActor` to track connection readiness — shows a \"Connecting to server…\" banner while the actor initializes, disables all form inputs and the submit button until the actor is ready, and displays a clear inline error if the connection is unavailable.",
  "frontend/src/components/ProfileScreen.tsx": "Settings screen with profile editing (display name, status), dark/light mode toggle switch using useTheme hook, and logout button — exported as default with onBack prop.",
  "frontend/src/components/ContactsList.tsx": "New chat screen with username search to find contacts and start one-on-one conversations.",
  "frontend/src/components/CreateGroupScreen.tsx": "Two-step group creation screen — participant selection via username search with checkboxes, then group name input and creation.",
  "frontend/src/components/ChatScreen.tsx": "Unified chat screen with sticky header (contact info + voice/video call buttons) and sticky input bar, scrollable message list, file upload, and VoiceCallOverlay/VideoCallOverlay integration — exported as default, uses named imports for all existing components, passes conversationId to useSendMessage hook.",
  "frontend/src/components/ChatListScreen.tsx": "Main chat list screen with sticky header, user search, conversation list, and FAB for new chat/group — removed the unsupported `isGroup` prop from `UserAvatar` usage to fix the TypeScript error.",
  "frontend/src/components/BottomNavigation.tsx": "New WhatsApp-style bottom navigation bar component with four tabs (Chats, Updates, Calls, Settings), each with an icon and label; the active tab is highlighted in the Xeta green accent color.",
  "frontend/src/components/UpdatesScreen.tsx": "Placeholder Updates tab screen with header and coming-soon message — exported as default, no footer copyright.",
  "frontend/src/components/CallsScreen.tsx": "Calls tab screen showing call history from localStorage with voice/video icons, contact names, duration, and relative timestamps — exported as default, uses named UserAvatar import.",
  "frontend/src/components/XetaAIChatScreen.tsx": "Full AI chat screen for Xeta AI interactions with rule-based/keyword responses, typing indicator animation, persistent chat history via localStorage, and a back button to return to the chat list.",
  "frontend/src/types/calls.ts": "TypeScript types for call history (CallType, CallDirection, CallHistoryEntry) with localStorage read/write utility functions.",
  "frontend/src/hooks/useTheme.ts": "Custom hook managing dark/light theme state with localStorage persistence, document class toggling, and an initTheme() function for pre-render flash prevention.",
  "frontend/src/components/VoiceCallOverlay.tsx": "Full-screen voice call overlay with WebRTC getUserMedia (audio), mute toggle, end-call button, running duration timer, and localStorage call history saving — uses named UserAvatar import.",
  "frontend/src/components/VideoCallOverlay.tsx": "Full-screen video call overlay with local video preview (PiP), WebRTC getUserMedia (audio+video), mute/camera toggle, end-call button, duration timer, and call history saving — uses named UserAvatar import."
}